interface:
  display_name: "planner-kernal"
  short_description: "Architecture-first JSON planning kernel with module contracts, executable checks, and recovery"
  default_prompt: "Use $planner-kernal to model replaceable modules and input/output contracts before creating or resuming an executable JSON plan for the current project."
policy:
  allow_implicit_invocation: false
