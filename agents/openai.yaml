interface:
  display_name: "planner-kernal"
  short_description: "Explicitly enabled JSON project planning kernel with per-module self-checks and cross-conversation recovery"
  default_prompt: "Use $planner-kernal to create or resume an executable JSON plan for the current project, and plan or implement according to my request."
policy:
  allow_implicit_invocation: false
