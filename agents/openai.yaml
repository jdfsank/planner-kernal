interface:
  display_name: "planner-kernal"
  short_description: "显式启用的 JSON 项目规划、逐模块自检与跨对话恢复内核"
  default_prompt: "使用 $planner-kernal 为当前项目建立或恢复可执行的 JSON 计划，并按我的请求规划或实施。"
policy:
  allow_implicit_invocation: false
